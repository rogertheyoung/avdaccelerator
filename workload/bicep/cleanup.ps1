Remove-AzResourceGroup -name 'rg-avd-mon-dev-euw-monitoring' -AsJob -Force
Remove-AzResourceGroup -name 'rg-avd-mon-dev-euw-network'  -AsJob -Force
Remove-AzResourceGroup -name 'rg-avd-mon-dev-euw-pool-compute' -AsJob -Force
Remove-AzResourceGroup -name 'rg-avd-mon-dev-euw-service-objects' -AsJob -Force
Remove-AzResourceGroup -name 'rg-avd-mon-dev-euw-storage' -AsJob -Force